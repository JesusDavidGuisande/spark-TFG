Spark:
  #codigo
  name: spark-job
  class: org.apache.spark.examples.SparkPi
  #Instancias de ejecucion que realizan el trabajo
  #todo memoria cpus
  executor-instances: 1
  #Opcional
  conf:
    - spark.kubernetes.context=minikube
  mount:
    -
     name: pv0001
     local-path: /home/noname/Escritorio/kb/volume
     cluster-path: /mnt/data

  image: jesusdavidguisande/spark:spark-v10
  jars:
      #local file etc.
    mode: local
      #Path de los jars en este casos e coge de forma local dentro de la maquina de minikube
    path: /mnt/data/spark-examples_2.12-3.1.2.jar
  #input-args:
  # - /mnt/data/listado-general.txt
